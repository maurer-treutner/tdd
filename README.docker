To use docker, change to the root directory of the project and build the docker image:
	$ docker build -t tdd_sample .

To run complete make under docker, call under unix opertaing systems:
	$ docker run --rm -v $(pwd):/project tdd_sample
